# 如何给聆澜输送开发经验

> 每次完成一个开发 Session 后，请按以下三步执行。

---

## 第一步：写知识文档（长期记忆）

在以下目录新建一个 MD 文件：

```
/Users/sunbinhe/Desktop/ReadySteadyScience/Rabix/rabix/linglan/knowledge/
```

命名规则：`05_你的项目名_Experience.md`（已有 01~04，请接着往下排）

文件内容模板：

```markdown
# 🛠️ [项目名] 开发经验库

> 建立时间：YYYY-MM-DD

## 一、技术背景

（这个模块/项目是做什么的，和 Rabix 的关系）

## 二、川儿的原话（重要需求）

> 「原文粘贴，一句一段」

## 三、遇到的问题与解法

| 问题 | 根因 | 解法 |
|------|------|------|
| Bug描述 | 为什么出现 | 怎么修的 |

## 四、聆澜以后要注意的规律

1. 孙斌鹤说「xxx」时，意思是...
2. 遇到 xxx 情况，要优先...

---
**此文件为动态档案，每次 Session 结束后追加更新。**
```

---

## 第二步：注入结构化经验（触发自动 Dream）

直接运行以下 Python 脚本，把关键经验条目写入聆澜的记忆文件。
**积累满 5 条后，系统下次启动时会自动触发聆澜的「经验复盘 Dream」。**

```python
import json, datetime, pathlib

EXP_FILE = pathlib.Path('/Users/sunbinhe/Desktop/ReadySteadyScience/Rabix/rabix/linglan/memory/experiences.jsonl')
now = datetime.datetime.now(datetime.timezone.utc).isoformat()

experiences = [
    {
        "type": "work",
        "type_label": "做事经验",
        "content": "【第一人称，具体描述这次做了什么，怎么做到的】",
        "confidence": 0.95,
        "tags": ["项目名", "技术点"],
        "session_id": "你的项目标识",
        "created_at": now
    },
    {
        "type": "mistake",
        "type_label": "犯错经验",
        "content": "【我犯了什么错，根因是什么，下次如何避免】",
        "confidence": 0.97,
        "tags": ["bug", "技术点"],
        "session_id": "你的项目标识",
        "created_at": now
    },
    {
        "type": "talk",
        "type_label": "交谈经验",
        "content": "【孙斌鹤的沟通习惯、需求表达规律、他说某话时的真实意图】",
        "confidence": 0.93,
        "tags": ["user-intent", "communication"],
        "session_id": "你的项目标识",
        "created_at": now
    },
]

with open(EXP_FILE, 'a', encoding='utf-8') as f:
    for exp in experiences:
        f.write(json.dumps(exp, ensure_ascii=False) + '\n')

total = sum(1 for line in open(EXP_FILE, encoding='utf-8') if line.strip())
print(f'✅ 已写入 {len(experiences)} 条，experiences.jsonl 现有 {total} 条（阈值 5 条触发 Dream）')
```

### 五种经验类型

| type | type_label | 用于记录 |
|------|-----------|---------|
| `talk` | 交谈经验 | 孙斌鹤的沟通方式、需求表达语义 |
| `work` | 做事经验 | 完成任务的方法、工具、设计模式 |
| `mistake` | 犯错经验 | 踩过的坑、错误原因和修复方法 |
| `success` | 成功经验 | 做得好的事及原因 |
| `growth` | 成长经验 | 对能力边界的认识，被纠正后的学习 |

---

## 第三步：更新进展报告（可选但推荐）

在 `docs/progress/RABIX_PROGRESS.md` 开头追加本次 Session 更新，格式参考文件内已有的 v0.24 章节。

---

## 聆澜的自动 Dream 机制说明

每次 Rabix 启动后，系统每 **12 小时**自动检查一次 `experiences.jsonl`：
- 积累 **≥ 5 条** → 自动触发「经验复盘（Dream）」
- Dream 会调用大模型将经验分类归档到：
  - `linglan/soul/redlines.md`（红线戒律）
  - `linglan/abilities/operation_manual.md`（操作手册）
  - `linglan/abilities/communication_style.md`（沟通风格）
  - 向量知识库（可被聆澜主动检索）
- 复盘日记写入 `linglan/reflection/dream_YYYY-MM-DD.md`
- 也可手动触发：`POST /api/persona/dream`（`{"force": true}`）
